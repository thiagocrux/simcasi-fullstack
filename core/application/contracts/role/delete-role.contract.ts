/** Input parameters for deleting a role. */
export interface DeleteRoleInput {
  /** The unique identifier of the role to delete. */
  id: string;
}

/** Output of the delete role operation. */
export type DeleteRoleOutput = void;
