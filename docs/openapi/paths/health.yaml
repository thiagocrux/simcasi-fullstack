check:
  get:
    operationId: getApiHealthStatus
    summary: API health status
    description: Verifies the operational status of the API service and its connection to the primary persistence layer (Database).
    tags:
      - System
    security: []
    responses:
      '200':
        description: Service operational and database connection verified (OK).
        content:
          application/json:
            schema:
              $ref: '../components/schemas/health.yaml#/HealthStatus'
      '503':
        description: Service unavailable — one or more dependencies are unreachable (Service Unavailable).
        content:
          application/json:
            schema:
              $ref: '../components/schemas/health.yaml#/HealthStatus'
