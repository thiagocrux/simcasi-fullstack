import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Lista de tratamentos | SIMCASI',
  description:
    'Visualize todos os tratamentos cadastrados, exclua registros ou acesse a página de edição e criação de tratamentos no SIMCASI.',
};

export default function TreatmentsPage() {
  return <p>TreatmentsPage</p>;
}
