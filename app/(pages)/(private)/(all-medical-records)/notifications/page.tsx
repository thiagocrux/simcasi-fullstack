import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Lista de notificações | SIMCASI',
  description:
    'Visualize todos as notificações cadastradas, exclua registros ou acesse a página de edição e criação de notificações no SIMCASI.',
};

export default function NotificationsPage() {
  return <p>NotificationsPage</p>;
}
