import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Lista de observações | SIMCASI',
  description:
    'Visualize todos as observações cadastradas, exclua registros ou acesse a página de edição e criação de observações no SIMCASI.',
};

export default function ObservationsPage() {
  return <p>ObservationsPage</p>;
}
