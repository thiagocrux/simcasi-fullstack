import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Lista de exames | SIMCASI',
  description:
    'Visualize todos os exames cadastrados, exclua registros ou acesse a página de edição e criação de exames no SIMCASI.',
};

export default function ExamsPage() {
  return <p>ExamsPage</p>;
}
