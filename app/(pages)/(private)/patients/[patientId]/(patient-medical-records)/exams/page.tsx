import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Exames do paciente | SIMCASI',
  description:
    'Visualize todos os exames cadastrados, exclua registros ou acesse a página de edição e criação de exames para este paciente no SIMCASI.',
};

export default function PatientExamsPage() {
  return <p>PatientExamsPage</p>;
}
