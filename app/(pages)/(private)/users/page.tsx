import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Lista de usuários | SIMCASI',
  description:
    'Visualize todos os usuários cadastrados, exclua registros ou acesse a página de edição e criação de usuários no Simcasi. Gerencie facilmente o acesso e os dados da sua equipe.',
};

export default function UsersPage() {
  return <p>UsersPage</p>;
}
